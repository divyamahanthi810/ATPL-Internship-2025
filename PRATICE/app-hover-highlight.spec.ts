import { AppHoverHighlight } from './app-hover-highlight';

describe('AppHoverHighlight', () => {
  it('should create an instance', () => {
    const directive = new AppHoverHighlight();
    expect(directive).toBeTruthy();
  });
});
