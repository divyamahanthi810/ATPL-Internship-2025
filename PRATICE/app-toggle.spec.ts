import { AppToggle } from './app-toggle';

describe('AppToggle', () => {
  it('should create an instance', () => {
    const directive = new AppToggle();
    expect(directive).toBeTruthy();
  });
});
