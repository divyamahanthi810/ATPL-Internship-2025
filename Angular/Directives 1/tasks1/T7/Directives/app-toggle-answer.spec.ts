import { AppToggleAnswer } from './app-toggle-answer';

describe('AppToggleAnswer', () => {
  it('should create an instance', () => {
    const directive = new AppToggleAnswer();
    expect(directive).toBeTruthy();
  });
});
