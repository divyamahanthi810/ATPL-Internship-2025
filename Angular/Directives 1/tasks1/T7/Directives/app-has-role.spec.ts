import { AppHasRole } from './app-has-role';

describe('AppHasRole', () => {
  it('should create an instance', () => {
    const directive = new AppHasRole();
    expect(directive).toBeTruthy();
  });
});
