import { HiglightOnHover } from './higlight-on-hover';

describe('HiglightOnHover', () => {
  it('should create an instance', () => {
    const directive = new HiglightOnHover();
    expect(directive).toBeTruthy();
  });
});
