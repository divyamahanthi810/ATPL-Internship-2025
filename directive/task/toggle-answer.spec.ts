import { ToggleAnswer } from './toggle-answer';

describe('ToggleAnswer', () => {
  it('should create an instance', () => {
    const directive = new ToggleAnswer();
    expect(directive).toBeTruthy();
  });
});
